import { DropdownOptionsPipe } from './dropdown-options-pipe';

describe('DropdownOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
